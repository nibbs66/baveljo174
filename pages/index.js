import React from 'react';
import Head from 'next/head'
import Schedule from "../components/Schedule"
import axios from 'axios'
import SmallSchedule from "../components/SmallSchedule";
import useGames from "./api/swr/useGames";
import Loader from "../components/Loader";
import GameView from "../components/GameView";
import SoccerBall from "../components/SoccerBall";
const Index = () => {
    const { games, isValidating} =  useGames()
    if( isValidating){
        return (
            <div className={`flex max-h-[calc(100vh - 128px)] w-screen  items-center justify-center`}>
                <span><SoccerBall/></span>
            </div>
        )
    }
    return (
        <>
            <Head>
                <title>Bavel JO17-4</title>
                <meta name="description" content="Generated by create next app" />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <div className={`hidden lg:flex md:items-center md:justify-center min-h-[calc(100vh-128px)] pt-24 pb-12 w-full`}>
               <Schedule  games={games} />
            </div>
            <div className={`h-screen w-full flex items-center justify-center lg:hidden`}>
                <div className={`md:w-1/2`}>
                    <GameView/>
                </div>

            </div>

        </>
    );
};

export default Index;
